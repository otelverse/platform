load("@bazel_gazelle//:def.bzl", "gazelle")

# gazelle:prefix github.com/otelverse/unified-platform
# gazelle:go_naming_convention import_alias
# gazelle:exclude packages/dev-desktop

gazelle(
    name = "gazelle",
)
